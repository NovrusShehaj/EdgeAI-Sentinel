# Large trained weights must use Git LFS or external artifact storage.
# Uncomment only when a real tracked weight file is added:
# *.onnx filter=lfs diff=lfs merge=lfs -text
# *.pt filter=lfs diff=lfs merge=lfs -text
# *.engine filter=lfs diff=lfs merge=lfs -text
