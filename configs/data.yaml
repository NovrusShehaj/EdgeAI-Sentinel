# Source of truth for class names. Serving and deployment must match.
# Images are synthetic smoke data generated by scripts/test_data.py.
# This is not a production evaluation set.
names:
- object
nc: 1
test: ../data/test/images
train: ../data/train/images
val: ../data/val/images
