# This file is not loaded by GitHub Actions.
# The active workflows live in:
#   .github/workflows/ci.yml
#   .github/workflows/deploy.yml
#
# Pull requests never deploy. Deployment is workflow_dispatch only and
# requires the protected production environment plus a populated inventory.
name: inactive-docs-workflow-stub
on:
  workflow_dispatch:
jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow path is inactive. Use .github/workflows/ci.yml"
